<mah:MetroWindow x:Class="Fateblade.Alexandria.UI.WPF.Client.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:prism ="http://prismlibrary.com/"
        xmlns:actionBar="clr-namespace:Fateblade.Alexandria.UI.WPF.Controls.ActionBar;assembly=Alexandria.UI.WPF.Controls"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="Alexandria" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="4*" />
        </Grid.ColumnDefinitions>

        <actionBar:ActionMenuBarControl Grid.Column="0" 
                                        MenuBarActions="{Binding Path=AvailableMenuActions}" />

        <GridSplitter Grid.Column="1" />

        <ContentPresenter Grid.Column="2" 
                          ContentTemplateSelector="{StaticResource ViewModelTemplateSelector}" 
                          Content="{Binding Path=CurrentlyDisplayedViewModel}" />
    </Grid>
</mah:MetroWindow>
