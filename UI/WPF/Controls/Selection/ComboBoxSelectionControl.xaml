<UserControl x:Class="Fateblade.Alexandria.UI.WPF.Controls.Selection.ComboBoxSelectionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:Background="White">
    <StackPanel Orientation="Horizontal">
        <Button Content="-" 
                Visibility="{Binding Path=AllowDeletion, 
                                     Converter={StaticResource Alexandria.Converters.BoolVisibleConverter},
                                     RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"
                Command="{Binding Path=DeleteCurrentElementCommand,  
                                  RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>

        <ComboBox Margin="4,0,0,0" 
                  Width="200"
                  DisplayMemberPath="Name"
                  ItemsSource="{Binding Path=SelectableElements,
                                        RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"
                  SelectedItem="{Binding Path=SelectedElement, Mode=TwoWay,
                                         RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>
        
        <Button Margin="4,0,0,0" 
                Content="+" 
                Visibility="{Binding Path=AllowCreation, 
                                     Converter={StaticResource Alexandria.Converters.BoolVisibleConverter},
                                     RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"
                Command="{Binding Path=CreateNewElementCommand,  
                                  RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"/>
    </StackPanel>
</UserControl>
