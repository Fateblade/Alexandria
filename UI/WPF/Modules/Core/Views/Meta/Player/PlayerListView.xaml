<UserControl x:Class="Fateblade.Alexandria.UI.WPF.Modules.Core.Views.Meta.PlayerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Fateblade.Alexandria.UI.WPF.Modules.Core.Views.Meta"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:meta="clr-namespace:Fateblade.Alexandria.UI.WPF.Models.Meta;assembly=Alexandria.UI.WPF.Models"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type meta:PlayerModel}">
                    <TextBlock Text="{Binding Path=Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="1"
                    Margin="0,4,0,0"
                    Orientation="Horizontal">

            <Button Margin="0,0,4,0"
                    Content="Add"
                    Command="{Binding Path=AddPlayerCommand}" />

            <Button Margin="0,0,0,0"
                    Content="Remove"
                    Command="{Binding Path=RemoveCurrentlySelectedPlayerCommand}"/>

        </StackPanel>
    </Grid>
</UserControl>
