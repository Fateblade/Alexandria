<UserControl x:Class="Fateblade.Alexandria.UI.WPF.Client.Dialogs.InfoDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" >
    <Grid>
         <Grid.RowDefinitions>
             <RowDefinition />
             <RowDefinition Height="Auto" />
         </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="4,4,4,0"
                   Text="{Binding Path=InfoText}" 
                   TextWrapping="Wrap" />

        <Button Grid.Row="1" Margin="0,8,4,4" 
                HorizontalAlignment="Right" 
                Content="Ok" 
                Command="{Binding Path=CloseDialogCommand}" />
    </Grid>
</UserControl>
