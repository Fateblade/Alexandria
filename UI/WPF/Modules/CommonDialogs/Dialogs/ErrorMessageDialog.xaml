<UserControl x:Class="Fateblade.Alexandria.UI.WPF.Client.Dialogs.ErrorMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d" Height="400" Width="300">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBox Text="{Binding Path=Message, Mode=OneWay}"  
                 TextWrapping="Wrap"
                 AcceptsTab="True" AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Visible" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Copy" 
                    Command="{Binding Path=CopyToClipboardCommand}" />

            <Button Margin="8,0,0,0"
                    Content="Close" 
                    Command="{Binding Path=CloseDialogCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
